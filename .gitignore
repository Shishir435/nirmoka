# Rust / Tauri
/target/
**/target/
Cargo.lock.bak

# Tauri's generated schemas and platform projects. Regenerated by every build;
# the shell lives in crates/app rather than the src-tauri/ the templates use.
/crates/app/gen/

# Node / frontend
node_modules/
dist/
.vite/
*.tsbuildinfo

# Vercel
.vercel/

# Environment
.env
.env.*
!.env.example

# Editors
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# macOS
.DS_Store

# Design reference images. Working material for a redesign, not source, and
# they are large binaries that would sit in history forever.
screen-ref/

# Local agent / assistant overrides
CLAUDE.local.md
AGENTS.local.md

# Generated Rust -> TS types are committed; their build artifacts are not.
!packages/transport/src/generated/
