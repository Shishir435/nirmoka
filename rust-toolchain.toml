[toolchain]
# Exact so local builds and every CI platform use the compiler this source was
# verified against rather than whatever "stable" means on the day they run.
channel = "1.97.1"
components = ["rustfmt", "clippy"]
